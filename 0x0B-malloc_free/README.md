0x0B. C - malloc, free

Task 0 - create_array
- Prototype: char *create_array(unsigned int size, char c);
- Returns NULL if size == 0 or if malloc fails.
- Allocates (size) bytes and fills them with c.
- Betty compliant.

